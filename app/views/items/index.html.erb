<h1> Items </h1>
<hr>
<div class="container" id="content">
  <% @items.each do |item| %>
    <%= link_to item.description, item %></br>
  <% end %>
<button class="btn btn-default btn-xs" style="float:left"><%= link_to 'New item', new_item_path(:list_id => @list.id) %></button> 
</div>